# Optional (enables STT/TTS + Vision OCR fallback)
OPENAI_API_KEY=

# CORS: add prod + preview vercel domains (comma-separated)
CORS_ALLOW_ORIGINS=https://YOUR-VERCEL-DOMAIN.vercel.app,https://YOUR-PREVIEW.vercel.app

# Storage on persistent disk
UPLOAD_DIR=/data/uploads
GENERATED_DIR=/data/generated

# OCR selection: auto | openai | tesseract
OCR_MODE=auto

